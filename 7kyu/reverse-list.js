https://www.codewars.com/kata/57a04da9e298a7ee43000111/javascript

// DESCRIPTION:
// Write reverseList function that simply reverses lists.


function reverseList(arr) {
    return arr.reverse();
  }

  /*
function reverseList(arr) {
    const reversedArr = [];
        for(let i = arr.length - 1; i >= 0; i--)
        reverseArray.push(arr[i]);
        return reversedArr;
  }

  */
 